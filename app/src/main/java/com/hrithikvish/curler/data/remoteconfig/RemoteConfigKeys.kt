package com.hrithikvish.curler.data.remoteconfig

// Central registry of Firebase Remote Config keys. Add one constant per
// feature key here rather than scattering string literals across repositories.
object RemoteConfigKeys {
    const val ABOUT_SCREEN = "aboutScreen"
}
